apiVersion: v1
kind: Service
metadata:
  name: interview-prep-api
  namespace: interview-prep
spec:
  selector:
    app: interview-prep-api
  ports:
    - port: 4000
      targetPort: 4000
  type: ClusterIP
