apiVersion: v1
kind: Service
metadata:
  name: interview-prep-frontend
  namespace: interview-prep
spec:
  selector:
    app: interview-prep-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
